<section class="pawly-page">
  <div class="pawly-container">
    <div class="pawly-section-head">
      <h2>{{ '404.title' | t }}</h2>
      <p>{{ '404.text' | t }}</p>
      <a class="pawly-btn pawly-btn--primary" href="{{ routes.root_url }}" style="margin-top:1.5rem">{{ '404.link' | t }}</a>
    </div>
  </div>
</section>

{% schema %}
{ "name": "404", "settings": [] }
{% endschema %}
